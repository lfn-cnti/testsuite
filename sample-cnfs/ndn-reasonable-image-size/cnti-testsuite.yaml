---
config_version: v2
common:
  # Far below the size of the Envoy Gateway image, so the test fails.
  image_size_max_mb: 16
deployments:
  helm_dirs:
  - name: envoy-gateway
    helm_directory: gateway-helm
